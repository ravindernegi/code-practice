let nameVar = "Ravi";
nameVar[0] = "Negi";
console.log(nameVar[0]);

/* In JavaScript, strings is immutable,which means you cannot change a string directly.
 * When you use string methods like slice(), toUpperCase(), replace(), etc.
 * they create a new string rather than modify the original string.
 */
